export default theme => ({
  buttonRow: {
    marginBottom: theme.spacing.unit * 6,
    marginTop: theme.spacing.unit * 4,
    textAlign: 'center',
  },
  descriptionRow: {
    marginTop: theme.spacing.unit * 4,
  },
});
